# ApexFlow - أداة معالجة ملفات PDF

> **أداة شاملة لمعالجة ملفات PDF باللغة العربية مع واجهة مستخدم حديثة**

[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://python.org)
[![PySide6](https://img.shields.io/badge/PySide6-6.6+-green.svg)](https://pypi.org/project/PySide6/)
[![License](https://img.shields.io/badge/License-EULA-red.svg)](docs/LICENSE.txt)
[![Version](https://img.shields.io/badge/Version-v5.2.1-orange.svg)](version.py)

## المميزات الرئيسية

### دمج ملفات PDF
- دمج عدة ملفات PDF في ملف واحد
- إضافة إشارات مرجعية للملفات المدموجة
- دمج صفحات محددة من ملفات مختلفة

### تقسيم ملفات PDF
- تقسيم PDF إلى صفحات منفردة
- تقسيم حسب نطاقات الصفحات
- تقسيم بعدد صفحات محدد لكل ملف
- استخراج صفحات معينة

### 🗜️ ضغط ملفات PDF
- 5 مستويات ضغط مختلفة
- ضغط مجمع لعدة ملفات
- إحصائيات مفصلة للتوفير في الحجم
- تحسين الصور والمحتوى

### 🔄 تدوير الصفحات والختم
- تدوير جميع الصفحات أو صفحات محددة
- زوايا تدوير متعددة (90°, 180°, 270°)
- إضافة أختام وعلامات مائية تفاعلية
- تدوير تلقائي للصفحات الأفقية
- معاينة مباشرة للتغييرات

### 🔄 تحويل الملفات
- تحويل PDF إلى صور (PNG, JPEG, TIFF)
- تحويل صور إلى PDF
- استخراج النص من PDF
- تحويل النص إلى PDF

### ⚙️ إعدادات متقدمة
- نظام سمات متطور (فاتح/داكن)
- ألوان تمييز قابلة للتخصيص
- حفظ ديناميكي أو في مسار ثابت
- إعدادات مخصصة لكل وظيفة
- اختصارات لوحة المفاتيح
- تصدير واستيراد الإعدادات

## متطلبات التشغيل

- Python 3.8 أو أحدث
- المكتبات المطلوبة:
  - PySide6 >= 6.6 (واجهة المستخدم)
  - PyPDF2 >= 3.0 (معالجة PDF)
  - PyMuPDF >= 1.23 (معالجة متقدمة)
  - Pillow >= 10.0 (معالجة الصور)
  - psutil (معلومات النظام)

## التثبيت والتشغيل

### 1. تثبيت المكتبات المطلوبة:
```bash
pip install -r requirements.txt
```

أو تثبيت المكتبات منفردة:
```bash
pip install "PySide6>=6.6" "PyPDF2>=3.0" "PyMuPDF>=1.23" "Pillow>=10.0" psutil
```

### 2. تشغيل التطبيق:

#### الطريقة الأولى - استخدام Python مباشرة:
```bash
python main.py
```

#### الطريقة الثانية - استخدام ملف batch (Windows):
```bash
run_app.bat
```

#### الطريقة الثالثة - استخدام مسار Python الكامل:
```bash
# مثال لمسار Python (قم بتعديله حسب مسار Python في جهازك)
C:\Users\[اسم_المستخدم]\AppData\Local\Programs\Python\Python313\python.exe main.py
```

## هيكل المشروع

```
ApexFlow/
├── main.py                    # الملف الرئيسي للتطبيق
├── run_app.bat               # ملف تشغيل سريع
├── version.py                # معلومات الإصدار
├── requirements.txt          # متطلبات المشروع
├── modules/                  # وحدات المعالجة
│   ├── __init__.py
│   ├── merge.py             # وحدة الدمج
│   ├── split.py             # وحدة التقسيم
│   ├── compress.py          # وحدة الضغط
│   ├── rotate.py            # وحدة التدوير
│   ├── convert.py           # وحدة التحويل
│   ├── settings.py          # وحدة الإعدادات
│   ├── app_utils.py         # أدوات التطبيق
│   ├── logger.py            # نظام السجلات
│   ├── stamp_processor.py   # معالج الأختام
│   └── coordinate_calibrator.py # معايرة الإحداثيات
├── ui/                      # واجهة المستخدم
│   ├── __init__.py
│   ├── apexflow_main_ui.py  # الواجهة الرئيسية
│   ├── theme_manager.py     # مدير السمات
│   ├── settings_ui.py       # واجهة الإعدادات
│   ├── rotate_page.py       # صفحة التدوير
│   ├── merge_page.py        # صفحة الدمج
│   ├── split_page.py        # صفحة التقسيم
│   ├── compress_page.py     # صفحة الضغط
│   ├── convert_page.py      # صفحة التحويل
│   ├── file_list_frame.py   # إطار قائمة الملفات
│   ├── svg_icon_button.py   # أزرار الأيقونات
│   ├── stamp_manager.py     # مدير الأختام
│   └── notification_system.py # نظام الإشعارات
├── assets/                  # الموارد
│   ├── icons/              # الأيقونات
│   └── menu_icons/         # أيقونات القوائم
├── tests/                   # اختبارات التطبيق
│   ├── unit/               # اختبارات الوحدات
│   ├── integration/        # اختبارات التكامل
│   └── gui/                # اختبارات الواجهة
├── docs/                    # التوثيق
│   ├── USER_GUIDE.md       # دليل المستخدم
│   ├── INSTALLATION_GUIDE.md # دليل التثبيت
│   └── LICENSE.txt         # الترخيص
└── export/                  # أدوات البناء والتصدير
    ├── build_scripts/      # سكريبتات البناء
    └── build_config.json   # إعدادات البناء
```

## الاستخدام

1. **تشغيل التطبيق**: استخدم إحدى طرق التشغيل المذكورة أعلاه
2. **اختيار الوظيفة**: انقر على الوظيفة المطلوبة من القائمة الجانبية
3. **اختيار الملفات**: استخدم أزرار اختيار الملفات
4. **تحديد الإعدادات**: اضبط الإعدادات حسب الحاجة
5. **تنفيذ العملية**: انقر على الزر المناسب لبدء المعالجة

## الإعدادات

يمكن تخصيص التطبيق من خلال قسم الإعدادات:

- **وضع الحفظ**: ديناميكي (اختيار مكان الحفظ في كل مرة) أو ثابت
- **مسار الحفظ الثابت**: تحديد مجلد افتراضي للحفظ
- **مستوى الضغط**: من 1 (أقل ضغط) إلى 5 (أقصى ضغط)
- **زاوية التدوير**: الزاوية الافتراضية للتدوير

## استكشاف الأخطاء

### مشكلة: "ModuleNotFoundError: No module named 'PySide6'"
**الحل**: تأكد من تثبيت المكتبات باستخدام:
```bash
pip install -r requirements.txt
```

### مشكلة: التطبيق لا يبدأ
**الحل**:
1. تأكد من إصدار Python (3.8 أو أحدث):
```bash
python --version
```

2. استخدم مسار Python الكامل إذا لزم الأمر:
```bash
# مثال (قم بتعديل المسار حسب جهازك)
C:\Users\[اسم_المستخدم]\AppData\Local\Programs\Python\Python313\python.exe main.py
```

### مشكلة: خطأ في معالجة ملف PDF
**الحل**: تأكد من أن الملف ليس محمياً بكلمة مرور وأنه ملف PDF صحيح

## التقنيات المستخدمة

تم تطوير هذا التطبيق باستخدام:
- **Python 3.8+** (مُختبر على Python 3.13)
- **PySide6 >= 6.6** للواجهة الرسومية الحديثة
- **PyPDF2 >= 3.0 & PyMuPDF >= 1.23** لمعالجة ملفات PDF
- **Pillow >= 10.0** لمعالجة الصور والتحويل
- **psutil** لمعلومات النظام والأداء
- **نظام سمات متطور** مع دعم الألوان المخصصة
- **أدوات تفاعلية** للأختام والعلامات المائية

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام والتطوير.

## الإصدارات المتاحة

يمكن الحصول على التطبيق بعدة أشكال:

### 1. الملف التنفيذي (.exe)
- ملف واحد قابل للتشغيل مباشرة
- لا يحتاج تثبيت Python
- سهل التوزيع والمشاركة

### 2. المثبت (Installer)
- تثبيت تلقائي مع اختصارات
- إضافة للقائمة Start Menu
- إمكانية إلغاء التثبيت

### 3. النسخة المحمولة (Portable)
- مجلد يحتوي على جميع الملفات
- لا يحتاج تثبيت
- يمكن تشغيله من USB

### 4. الكود المصدري
- للمطورين والمساهمين
- إمكانية التعديل والتطوير
- يتطلب تثبيت Python والمكتبات

---

**ملاحظات مهمة**:
- التطبيق يدعم اللغة العربية بالكامل في الواجهة والرسائل
- يعمل على Windows, Linux, و macOS
- واجهة مستخدم حديثة مع نظام سمات متطور
- دعم كامل للملفات العربية وأسماء المجلدات العربية
